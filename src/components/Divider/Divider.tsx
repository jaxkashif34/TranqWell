import React from 'react';
import { SView } from '~base';

type Props = {};

export const Divider = (props: Props) => {
  return <SView className="border-b border-disabled my-1 rounded-full"></SView>;
};
